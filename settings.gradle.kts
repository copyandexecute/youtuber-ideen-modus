rootProject.name = "youtuber-ideen-modus"

pluginManagement {
    repositories {
        gradlePluginPortal()
        // PaperMC Repo
        maven("https://papermc.io/repo/repository/maven-public/")
    }
}
